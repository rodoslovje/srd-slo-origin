{
  "name": "slovenian-dna",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "node -e \"require('fs').writeFileSync('./public/version.json', JSON.stringify({ buildDate: new Date().toISOString() }))\" && vite build && cp -r data dist/",
    "preview": "vite preview",
    "format": "prettier --write \"src/**/*.js\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "vite": "^5.2.0"
  }
}
